{
  "name": "testpre",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --base=/testpre/ --outDir=docs",
    "build": "tsc && vite build --base=/testpre/",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.19.6"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.2",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
